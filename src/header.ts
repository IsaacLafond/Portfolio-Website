export class WindowHeader extends HTMLElement {
    constructor() {
        super()
        // contains main div that spans whole width with height of ___?
        // 3 dot left justified
        // 
    }
}